Scientific task (PUR-RECOVER V1, blinded decision recovery)

You are given a finite candidate space of polyurethane prepolymer formulations. Every candidate is a two-component polyol blend (100 parts polyol basis) reacted with a diisocyanate at a stoichiometric ratio NCO:OH. For every candidate the admissible table contains the response and formulation descriptors allowed by the frozen benchmark.

The deterministic scientific workflow has already frozen (a) the property objective, (b) the nominal hard constraints, (c) the robust decision definition and (d) the backward-design and local-trend definitions. They are reproduced verbatim below. You must not change or reinterpret them.

Recover the complete decision chain:
1. property-only winner (objective only, no constraints);
2. constrained winner (objective among candidates passing every nominal hard constraint);
3. robust result: return the robust winner if the robust-feasible set is non-empty; if the frozen robust definition produces an empty set, return no winner and explicitly report that the robust target is unreachable under the frozen definition; if the robust layer itself is not computable from the admissible data, abstain and state the missing dependency;
4. the active constraint that separates the property-only winner from the deployable decision (name, threshold, candidate value);
5. the backward design: the continuous minimum NCO:OH at which the property-only winner's blend satisfies the active constraint, the nearest reachable NCO:OH grid value on the discrete design manifold, and whether it is reachable;
6. local trends at the final deployable decision point: the direction of eta80 as NCO:OH increases along the same blend, and the direction of eta80 as the parts of the data-defined composition axis component increase at fixed NCO:OH (name the axis component you used).

Task descriptor:
{{TASK_JSON}}

Frozen definitions:
{{CONFIG_JSON}}

{{PROVENANCE_BLOCK}}

Do not request or infer hidden source identifiers, oracle ranks, scores, gold files, mapping files or wet-lab results. Every number in your answer must come from the frozen definitions applied to the admissible table.
