# Copy-paste prompt for local Claude — PUR-RECOVER V1

You are taking over the **current Agent implementation and formal benchmark phase** of the repository:

`https://github.com/stloendays/PUR-Essay`

Work directly in a fresh clone or pull the latest `main` before doing anything.

## Critical version warning

There are two different historical Agent code paths in this repository.

**CURRENT PAPER AGENT:**

- `src/pur_agent/`
- benchmark name: `PUR_RECOVER_V1`
- role: blinded scientific decision recovery

**OLD / HISTORICAL AGENT — DO NOT CONTINUE THE CURRENT PAPER FROM THIS:**

- `src/pur_bridge/agent.py`
- E6 / `E6_star`
- information-gain / active-learning experiment selection
- material-equivalence-gate-led experiment choice

If any previous work you did was based on E6, information gain, active learning, or `src/pur_bridge/agent.py`, treat that work as historical only. Do not delete it, but do not use it as the current Agent architecture.

## Current paper architecture

The scientific answer is not generated by the LLM.

```text
source-grounded real rheology
 -> deterministic PUR-FRONTIER V1
 -> freeze evaluator gold
 -> hide answer / anonymise candidates
 -> PUR-RECOVER V1 Agent
 -> independent evaluator
 -> prospective wet-lab validation later
```

The Agent is therefore a **blinded scientific decision-recovery system**, not an optimizer that owns the ground truth and not an active-learning experiment selector.

The deterministic paper core is already frozen. Do not redesign it just because another formulation would make the Agent look better.

## First: read these canonical files in this order

1. `README.md`
2. `docs/AGENT_HANDOFF_CURRENT.md`
3. `docs/AGENT_STRATEGY_V1.md`
4. `configs/recover_v1.json`
5. `configs/frontier_v1.json`
6. `docs/FRONTIER_V1.md`
7. `docs/FRONTIER_DEPTH_V1.md`
8. `docs/PUR_SIM_V1_RECOVERY_AUDIT.md`
9. `manuscript/NON_AGENT_RESULTS_V2.md`
10. `src/pur_agent/`
11. `src/pur_science/`
12. `scripts/build_blind_bundle.py`
13. `scripts/run_agent_once.py`
14. `scripts/run_agent_benchmark.py`
15. `scripts/run_baselines.py`
16. `scripts/evaluate_agent_runs.py`
17. `scripts/summarize_benchmark.py`
18. `scripts/verify_no_leakage.py`
19. `prompts/recover_v1_system.txt`
20. `prompts/recover_v1_task.txt`
21. `tests/test_recover_v1.py`
22. `tests/test_no_gold_leakage.py`
23. `tests/test_mock_end_to_end.py`

Then inspect `src/pur_bridge/agent.py` only to confirm that it is the historical E6 path. Do not port its experiment-selection logic into `pur_agent`.

## Scientific contract that you must preserve

The primary benchmark must hide evaluator-only answers from the model-under-test.

Developer/evaluator-side frozen truth is already encoded by the deterministic science layer. Never inject source IDs, gold ranks, oracle scores, best flags, evaluator mapping, or future wet-lab results into the primary Agent context.

The model-under-test should receive only the approved generated blind bundle under:

`benchmark/recover_v1/blind/`

plus the approved prompt and deterministic tool interfaces.

It must not have tool/filesystem access to:

- `benchmark/recover_v1/evaluator_only/`
- `gold/recover_v1/`
- `results/frontier_v1/`
- `results/frontier_depth_v1/`
- manuscripts that print the gold candidate IDs
- `data/prospective_validation/`

The developer process may use evaluator files after the Agent run for scoring. The model-under-test may not.

## What the Agent must recover

A full successful run must recover the complete decision chain, not just one candidate:

1. property-only winner;
2. nominal constrained winner;
3. robust winner;
4. active constraint;
5. continuous backward threshold;
6. first reachable grid value and reachability verdict;
7. local NCO direction;
8. local composition direction.

Primary metric:

`complete_decision_recovery`

Secondary/diagnostic metrics:

- Top-1 / Top-3 / Top-5;
- recovered deterministic rank;
- objective regret;
- hard-constraint violations;
- backward-threshold absolute error;
- reachability accuracy;
- explanation fidelity;
- tool calls;
- API calls;
- input/output tokens;
- latency;
- invalid JSON/schema failures;
- transport/time-out failures.

## Required experimental conditions

Preserve the currently declared benchmark conditions:

- `oracle`
- `direct_llm`
- `tool_llm`
- `pur_agent`
- `pur_agent_no_backward`
- `pur_agent_no_constraint_checker`
- `pur_agent_no_provenance`
- `pur_agent_single_pass`

The deterministic `oracle` is the gold reference, not an LLM competitor.

Do not remove negative-result conditions because they perform poorly. Do not tune prompts/constraints separately after seeing formal results.

## Your job

### Phase A — audit before changing code

Perform a repository audit and create a short report identifying:

- which current PUR-RECOVER components are already fully implemented;
- which pieces are partial or broken;
- any remaining references that accidentally route the current benchmark into old E6/active-learning logic;
- any possible gold leakage path;
- any discrepancy between `docs/AGENT_STRATEGY_V1.md`, `configs/recover_v1.json`, prompts, schemas, tools, evaluator and run scripts;
- whether the provider/API transport works with the endpoint I will configure through environment variables.

Do not rewrite working components unnecessarily.

### Phase B — reproduce deterministic science

Run:

```bash
git pull origin main
python -m venv .venv
# activate the environment for the current OS
pip install -e ".[agent,dev]"
pytest -q
python scripts/freeze_frontier_v1.py
```

The 928-row PUR_SIM_V1 response table must be reconstructed from the frozen snapshot and hash-verified. If any hash/count/frozen decision test fails, STOP and report it. Do not synthesize missing responses and do not proceed to real API runs.

### Phase C — build and inspect the blind benchmark

Run:

```bash
python scripts/build_blind_bundle.py
python scripts/verify_no_leakage.py
```

Then manually audit the generated primary blind directory for leakage. Specifically search for:

- `WO_INV_`
- source polyol labels if they were supposed to be anonymised
- `oracle_score`
- `oracle_rank`
- `oracle_is_best`
- `gold_candidate_id`
- evaluator mapping content
- explicit gold winner text
- future wet-lab results

Document the blind-bundle file list, hashes and anonymisation seed.

### Phase D — offline infrastructure smoke

Before spending API calls, run:

```bash
python scripts/run_agent_once.py --provider mock --condition pur_agent
python scripts/run_baselines.py --runs 2 --provider mock
python scripts/evaluate_agent_runs.py results/recover_v1
python scripts/summarize_benchmark.py
```

Mock output is infrastructure validation only. Never report it as LLM performance in the paper.

### Phase E — API integration

The real API credential will be configured locally by me. Never ask me to paste the key into source code, a prompt, Git history or benchmark JSON.

Use environment variables only:

```text
OPENAI_API_KEY
OPENAI_BASE_URL   # optional; for an OpenAI-compatible endpoint
OPENAI_MODEL
OPENAI_PROVIDER   # optional
```

Before formal benchmarking, run one minimal API connectivity test and one full `pur_agent` pilot. Record model identifier, provider/endpoint type, response IDs if available, API usage, latency and transport errors. Do not log the secret.

If an OpenAI-compatible provider does not support one API feature, adapt only the transport/client compatibility layer. Do not change scientific tools, prompts, objective, constraints or evaluator to accommodate the provider.

### Phase F — pilot benchmark

Start with 5 independent real runs per core condition:

```bash
python scripts/run_baselines.py \
  --conditions direct_llm tool_llm pur_agent \
  --runs 5
```

Also run the deterministic oracle once.

Use a declared seed schedule. Preserve all failures. Do not delete a run because it returns invalid JSON, times out, violates constraints or selects the wrong candidate.

Produce a pilot report with:

- complete decision recovery rate;
- Top-1 recovery;
- backward/reachability recovery;
- constraint violation rate;
- explanation fidelity;
- token/API/latency cost;
- failure taxonomy;
- exact git commit and all relevant hashes.

If the pilot exposes an implementation bug, fix the bug before formal runs and restart all affected conditions. If it merely exposes a weak model, keep the negative result.

### Phase G — freeze formal benchmark

Once the pilot infrastructure is correct:

1. freeze the git commit;
2. freeze prompt hashes;
3. freeze config hashes;
4. freeze blind-data hashes;
5. freeze anonymisation seed;
6. freeze model/provider identifiers;
7. freeze seed schedule;
8. freeze max-round/tool policy;
9. write a preregistration-style run manifest before executing formal runs.

Do not change these after seeing formal performance unless you explicitly version a new benchmark.

### Phase H — formal repeated runs

Target 30–50 independent runs per LLM condition/configuration if API cost permits. At minimum, make the same number of runs for comparable LLM conditions.

Core conditions:

```text
direct_llm
tool_llm
pur_agent
```

Ablations:

```text
pur_agent_no_backward
pur_agent_no_constraint_checker
pur_agent_no_provenance
pur_agent_single_pass
```

Run records must be immutable raw evidence. Aggregated summaries can be regenerated later.

### Phase I — evaluation and Figure 7 data

After runs finish:

```bash
python scripts/evaluate_agent_runs.py results/recover_v1
python scripts/summarize_benchmark.py
```

Create manuscript-ready CSV/JSON for Figure 7 containing at least:

- condition;
- model;
- N runs;
- complete-decision recovery rate;
- Top-1 rate;
- mean/median regret;
- constraint violation rate;
- backward threshold success/error;
- reachability accuracy;
- explanation fidelity;
- median tool calls;
- median API calls;
- median tokens;
- median latency;
- invalid/schema/API failure count.

Also create a per-run long-format table so uncertainty intervals/bootstrap CIs can be rendered later in R.

Do NOT generate fake Agent performance and do not use mock results in Figure 7.

## Strong methodological requirement

Treat this like the Agent harness in a serious scientific benchmark:

- deterministic science is the judge;
- model-under-test is the player;
- the player cannot inspect the answer key;
- winner-only accuracy is insufficient;
- the complete decision chain matters;
- negative results remain part of the study;
- agentic planning/tool use must be separable from deterministic calculation;
- run cost and failures are first-class metrics;
- prospective laboratory outcomes are never used to tune the benchmark.

## Do not do the following

- Do not revive E6 as the current experiment-selection task.
- Do not use information gain as the main current Agent objective.
- Do not perform active learning over the 928 candidates.
- Do not let the LLM alter the frozen objective/constraints.
- Do not show `WO_INV_0419`, `WO_INV_0579`, `WO_INV_0420` to the model-under-test.
- Do not give the primary Agent named PPG chemistry unless running the explicitly secondary named-chemistry benchmark.
- Do not use manuscript text containing the answers as retrieval context.
- Do not add wet-lab data to the blind bundle.
- Do not silently replace failed runs.
- Do not commit API keys.

## Final deliverables back to me

When finished, return:

1. a concise audit of old vs current Agent architecture;
2. every changed file and why it changed;
3. exact commands used;
4. test/leakage results;
5. API transport status;
6. pilot benchmark summary;
7. formal run manifest if ready;
8. formal benchmark results if executed;
9. failure taxonomy;
10. Figure 7-ready CSV/JSON paths;
11. the exact git commit SHA;
12. any scientific-contract issue that would require a new benchmark version rather than a patch.

Do not claim the Agent phase is complete merely because one successful run recovered the winner.