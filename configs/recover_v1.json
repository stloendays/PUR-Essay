{
  "benchmark_id": "PUR_RECOVER_V1",
  "science_definition_from": "configs/frontier_v1.json",
  "note": "All scoring rules (columns, hard_constraints, objective, robustness, backward, local_trends) are inherited from configs/frontier_v1.json at load time. Do not duplicate them here.",
  "anonymization": {
    "component_columns": ["PPG400", "PPG700", "PPG1000", "PPG2000"],
    "material_prefix": "Polyol",
    "seed": 20260909
  },
  "blind": {
    "drop_columns": ["oracle_score", "oracle_rank", "oracle_is_best", "gold_candidate_id", "oracle_feasible", "oracle_interval_inside_broad", "broad_margin"]
  },
  "required_outputs": [
    "property_winner",
    "constrained_winner",
    "robust_winner_or_explicit_abstention",
    "active_constraint",
    "backward_design",
    "local_trends"
  ],
  "evaluation": {
    "backward_threshold_tolerance_nco_oh": 0.03,
    "top_k": [1, 3, 5],
    "primary_metric": "complete_decision_recovery"
  },
  "secondary_benchmarks": {
    "named_chemistry": "build with --no-anonymize to test performance with domain priors; never the primary result"
  }
}
